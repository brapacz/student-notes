- breadcrumb :payments

%table#payments.table.table-condensed.datatable.display
  %thead
    %tr
      %th= t('student.columns.first_name')
      %th= t('student.columns.last_name')
      %th= t('student.columns.amount')
      %th= t('student.columns.received_at')
  %tbody
